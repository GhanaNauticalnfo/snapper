# Database Configuration
NODE_ENV=development
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=ghanawaters_user
DATABASE_PASSWORD=ghanawaters_password
DATABASE_NAME=ghanawaters_db

# MQTT Configuration (NanoMQ)
MQTT_BROKER_URL=mqtt://localhost:1883
MQTT_API_PASSWORD=mqtt_api_password
MQTT_ENABLED=true

# Keycloak Configuration (Optional - disabled by default)
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin_password
KEYCLOAK_HOSTNAME=localhost

# Alternative Database URL format (uncomment to use)
# DATABASE_URL=postgresql://ghanawaters_user:ghanawaters_password@localhost:5432/ghanawaters_db