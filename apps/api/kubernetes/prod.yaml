apiVersion: apps/v1
kind: Deployment
metadata:
  name: snapper-api
  namespace: prod
  labels:
    app: snapper-api
    env: prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: snapper-api
      env: prod
  template:
    metadata:
      labels:
        app: snapper-api
        env: prod
    spec:
      containers:
      - name: snapper-api
        image: ghananauticalinfo/snapper-api:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: snapper-postgres-secret
              key: DATABASE_URL
---
apiVersion: v1
kind: Service
metadata:
  name: snapper-api-service
  namespace: prod
spec:
  selector:
    app: snapper-api
    env: prod
  ports:
  - port: 80
    targetPort: 3000
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: snapper-api-ingress
  namespace: prod
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - snapper-api.ghananautical.info
    secretName: prod-snapper-api-tls
  rules:
  - host: snapper-api.ghananautical.info
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: snapper-api-service
            port:
              number: 80